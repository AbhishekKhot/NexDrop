{
  "name": "nexdrop-backend",
  "version": "1.0.0",
  "description": "NexDrop relay — encrypted WebSocket relay for browser-to-browser instant file sharing",
  "main": "dist/relay.js",
  "type": "commonjs",
  "scripts": {
    "dev": "nodemon",
    "build": "tsc",
    "start": "node dist/relay.js",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "//": "LAN-only deps (bonjour-service, uuid, supertest, @types/supertest, @types/uuid) were dropped when the LAN agent was disabled. Re-install if you re-enable the LAN code under backend/src/{api,discovery,transport,chunking,crypto}.",
  "dependencies": {
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^22.15.29",
    "@types/ws": "^8.18.1",
    "jest": "^30.3.0",
    "nodemon": "^3.1.10",
    "ts-jest": "^29.4.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
